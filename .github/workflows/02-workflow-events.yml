name: 02 - Eventos
on:
  - push
  - workflow_dispatch

jobs:
  saludo: 
    runs-on: ubuntu-latest
    steps:

      - name: ShowTrigger
        run: |
          echo "Event name: ${{ github.event_name }}"
